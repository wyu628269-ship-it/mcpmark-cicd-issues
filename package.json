{
  "name": "mcpmark-cicd",
  "version": "1.0.0",
  "description": "Node.js project with PR automation workflow",
  "main": "index.js",
  "scripts": {
    "lint": "eslint . --ext .js",
    "test": "jest --coverage",
    "build": "echo 'Build successful'"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^2.0.0",
    "jest": "^29.0.0"
  },
  "jest": {
    "collectCoverage": true,
    "coverageDirectory": "coverage"
  }
}